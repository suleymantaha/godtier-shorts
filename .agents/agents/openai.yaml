interface:
  display_name: "GodTier Shorts"
  short_description: "GodTier Shorts repo workflow"
  default_prompt: "Use $godtier-shorts to work on this repo's pipeline, subtitle, auth, or workflow code safely."
